<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorSecondary</item>
        <item name="android:textColor">@color/true_black</item>
        <item name="android:textSize">@dimen/text_medium</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- BUTTONS -->
    <style name="CTA" parent="AppTheme">
        <item name="android:paddingTop">@dimen/medium</item>
        <item name="android:paddingBottom">@dimen/medium</item>
        <item name="android:paddingStart">@dimen/x_large</item>
        <item name="android:paddingEnd">@dimen/x_large</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/text_medium</item>
    </style>

    <style name="CTA.Filled">
        <item name="android:background">@drawable/button_states</item>
        <item name="android:textColor">@color/true_black</item>
    </style>

    <!-- EDIT TEXT STYLES -->
    <style name="FormField" parent="AppTheme">
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingBottom">@dimen/medium</item>
    </style>

    <style name="FormFieldLayout" parent="AppTheme">
        <item name="android:textColorHint">@color/hintColor</item>
        <item name="hintTextAppearance">@style/FormFieldHint</item>
    </style>

    <style name="FormFieldHint" parent="AppTheme">
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:textColor">@color/hintColor</item>
    </style>

    <style name="Subtitle" parent="AppTheme">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/text_medium_large</item>
    </style>

    <style name="CategoryTitle" parent="AppTheme">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/text_medium</item>
    </style>
</resources>
